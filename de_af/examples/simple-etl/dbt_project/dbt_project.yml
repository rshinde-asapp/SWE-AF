name: 'simple_etl'
version: '1.0.0'
config-version: 2

profile: 'simple_etl'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  simple_etl:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
